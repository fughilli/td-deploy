# build/run artifacts
out/
out_*/
out_*
result
result-*

# bazel
bazel-*
/.bazel/
.bazel-disk-cache/
.bazel-repo-cache/

# secrets — NEVER commit deploy keys (sbc-deploy generates these locally)
deploy/secrets/
*deploy_key*
secrets/

# sbc-deploy build scratch (builder VM logs, gc roots)
.sbc-build/

# python
__pycache__/
*.pyc

# nix/direnv
.direnv/

# mlir compiler build
compiler/build/

# rust runtime
runtime_rs/target/
runtime_rs/*.png

# td-deploy Studio app build outputs (all large / per-OS / generated)
app/dist/
app/build/
app/toolchain/
app/version.json
app/electron/node_modules/
app/electron/dist/
__pycache__/
*.pyc
